version: "3.4"

services:
  mainapi:
    build:
      target: development
    image: main-api-dev
    volumes:
      - ./:/usr/src/app
    environment:
      ENVIRONMENT: production
      MONGO_AZURE_URI: ${MONGO_AZURE_URI}
    ports:
      - "8000:5000"
